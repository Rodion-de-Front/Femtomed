# Полный план настройки отправки заявок на почту

## 📋 Обзор

В проекте уже реализована функциональность отправки email через `nodemailer`. Для работы необходимо настроить переменные окружения и выбрать email-провайдера.

---

## 🎯 Шаг 1: Выбор email-провайдера

### Вариант 1: Gmail (рекомендуется для начала)

- **Плюсы**: Простая настройка, бесплатно
- **Минусы**: Требует "Пароль приложения", ограничения на количество писем
- **SMTP настройки**:
  - Host: `smtp.gmail.com`
  - Port: `587` (TLS) или `465` (SSL)
  - Secure: `false` для 587, `true` для 465

### Вариант 2: Yandex Mail

- **Плюсы**: Российский провайдер, надежный
- **Минусы**: Требует настройки пароля приложения
- **SMTP настройки**:
  - Host: `smtp.yandex.ru`
  - Port: `465` (SSL) или `587` (TLS)
  - Secure: `true` для 465, `false` для 587

### Вариант 3: Mail.ru

- **Плюсы**: Российский провайдер
- **Минусы**: Требует настройки пароля приложения
- **SMTP настройки**:
  - Host: `smtp.mail.ru`
  - Port: `465` (SSL) или `587` (TLS)
  - Secure: `true` для 465, `false` для 587

### Вариант 4: SendGrid (для продакшена)

- **Плюсы**: Профессиональный сервис, высокие лимиты, хорошая доставляемость
- **Минусы**: Платный после бесплатного тарифа (100 писем/день бесплатно)
- **SMTP настройки**:
  - Host: `smtp.sendgrid.net`
  - Port: `587`
  - Secure: `false`
  - User: `apikey`
  - Password: API ключ из SendGrid

### Вариант 5: Mailgun

- **Плюсы**: Профессиональный сервис, хорошая доставляемость
- **Минусы**: Платный после бесплатного тарифа
- **SMTP настройки**:
  - Host: `smtp.mailgun.org`
  - Port: `587`
  - Secure: `false`

---

## 🔧 Шаг 2: Настройка переменных окружения

Создайте файл `.env` в корне проекта со следующими переменными:

### Для Gmail:

```env
# Email настройки
EMAIL_TO=office@femtomed.ru
EMAIL_FROM=noreply@femtomed.ru
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=ваш_email@gmail.com
EMAIL_PASSWORD=ваш_пароль_приложения

# Telegram (опционально, можно оставить пустым)
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_IDS=
```

### Для Yandex:

```env
EMAIL_TO=office@femtomed.ru
EMAIL_FROM=noreply@femtomed.ru
EMAIL_HOST=smtp.yandex.ru
EMAIL_PORT=465
EMAIL_USER=ваш_email@yandex.ru
EMAIL_PASSWORD=ваш_пароль_приложения
```

### Для Mail.ru:

```env
EMAIL_TO=office@femtomed.ru
EMAIL_FROM=noreply@femtomed.ru
EMAIL_HOST=smtp.mail.ru
EMAIL_PORT=465
EMAIL_USER=ваш_email@mail.ru
EMAIL_PASSWORD=ваш_пароль_приложения
```

### Для SendGrid:

```env
EMAIL_TO=office@femtomed.ru
EMAIL_FROM=noreply@femtomed.ru
EMAIL_HOST=smtp.sendgrid.net
EMAIL_PORT=587
EMAIL_USER=apikey
EMAIL_PASSWORD=ваш_sendgrid_api_ключ
```

---

## 🔐 Шаг 3: Получение пароля приложения (для Gmail/Yandex/Mail.ru)

### Gmail:

1. Войдите в аккаунт Google
2. Перейдите в [Настройки аккаунта](https://myaccount.google.com/)
3. Включите двухфакторную аутентификацию (если не включена)
4. Перейдите в [Пароли приложений](https://myaccount.google.com/apppasswords)
5. Создайте новый пароль приложения для "Почта"
6. Используйте этот пароль в `EMAIL_PASSWORD`

### Yandex:

1. Войдите в аккаунт Yandex
2. Перейдите в [Настройки безопасности](https://id.yandex.ru/security)
3. Включите двухфакторную аутентификацию (если не включена)
4. Создайте пароль приложения для "Почтовый клиент"
5. Используйте этот пароль в `EMAIL_PASSWORD`

### Mail.ru:

1. Войдите в аккаунт Mail.ru
2. Перейдите в [Настройки безопасности](https://id.mail.ru/profile)
3. Включите двухфакторную аутентификацию
4. Создайте пароль приложения
5. Используйте этот пароль в `EMAIL_PASSWORD`

---

## 📝 Шаг 4: Настройка SendGrid (если выбран этот вариант)

1. Зарегистрируйтесь на [SendGrid](https://sendgrid.com/)
2. Создайте API ключ:
   - Settings → API Keys → Create API Key
   - Выберите "Full Access" или "Mail Send"
   - Скопируйте ключ (он показывается только один раз!)
3. Используйте в `.env`:
   - `EMAIL_USER=apikey`
   - `EMAIL_PASSWORD=ваш_api_ключ`
4. Верифицируйте отправителя (Sender Verification):
   - Settings → Sender Authentication
   - Добавьте email адрес отправителя

---

## ✅ Шаг 5: Проверка настроек

### 5.1. Убедитесь, что файл `.env` находится в корне проекта

```
/Users/rodik/Desktop/Femtomed/.env
```

### 5.2. Проверьте, что `.env` добавлен в `.gitignore`

```gitignore
.env
.env.local
.env.*.local
```

### 5.3. Перезапустите сервер

```bash
npm run dev
```

### 5.4. Протестируйте отправку

1. Откройте страницу контактов: `http://localhost:6464/contacts`
2. Заполните форму обратной связи
3. Отправьте форму
4. Проверьте почту `office@femtomed.ru` (или указанную в `EMAIL_TO`)

---

## 🐛 Шаг 6: Решение проблем

### Проблема: "Email не настроен: отсутствуют учетные данные"

**Решение**: Проверьте, что все переменные `EMAIL_*` заполнены в `.env`

### Проблема: "Invalid login" или "Authentication failed"

**Решение**:

- Для Gmail/Yandex/Mail.ru: используйте пароль приложения, а не обычный пароль
- Проверьте правильность email в `EMAIL_USER`
- Убедитесь, что двухфакторная аутентификация включена

### Проблема: "Connection timeout"

**Решение**:

- Проверьте правильность `EMAIL_HOST` и `EMAIL_PORT`
- Проверьте настройки файрвола
- Попробуйте другой порт (587 вместо 465 или наоборот)

### Проблема: "Self-signed certificate"

**Решение**: Добавьте в `server/contact.ts` опцию `tls: { rejectUnauthorized: false }` (только для разработки!)

### Проблема: Письма попадают в спам

**Решение**:

- Используйте профессиональный сервис (SendGrid, Mailgun)
- Настройте SPF, DKIM, DMARC записи для домена
- Используйте доменную почту вместо Gmail/Yandex

---

## 🔒 Шаг 7: Безопасность (важно!)

1. **Никогда не коммитьте `.env` в Git**

   - Убедитесь, что `.env` в `.gitignore`

2. **Используйте пароли приложений**

   - Не используйте основной пароль от email

3. **Ограничьте доступ к `.env`**

   - Права доступа: `chmod 600 .env` (только владелец может читать)

4. **Для продакшена используйте переменные окружения хостинга**
   - Не загружайте `.env` на сервер
   - Используйте настройки хостинга (Heroku, Vercel, Railway и т.д.)

---

## 📊 Шаг 8: Мониторинг и логирование

Текущая реализация уже включает:

- ✅ Логирование ошибок в консоль
- ✅ Обработку ошибок отправки
- ✅ Возврат статуса успеха/ошибки клиенту

### Дополнительные улучшения (опционально):

1. Сохранение заявок в базу данных
2. Отправка уведомлений администратору при ошибках
3. Очередь отправки писем (для больших объемов)
4. Шаблоны писем (HTML с брендингом)

---

## 🚀 Шаг 9: Деплой на продакшен

### Для Vercel/Netlify:

1. Добавьте переменные окружения в настройках проекта
2. Используйте те же названия переменных (`EMAIL_TO`, `EMAIL_HOST` и т.д.)

### Для VPS/сервера:

1. Создайте `.env` файл на сервере
2. Заполните все переменные
3. Установите права: `chmod 600 .env`
4. Перезапустите приложение

### Для Docker:

1. Используйте `docker-compose.yml` с переменными окружения
2. Или передавайте через `-e` флаги

---

## 📋 Чек-лист настройки

- [ ] Выбран email-провайдер
- [ ] Создан файл `.env` в корне проекта
- [ ] Заполнены все переменные `EMAIL_*`
- [ ] Получен пароль приложения (для Gmail/Yandex/Mail.ru)
- [ ] `.env` добавлен в `.gitignore`
- [ ] Сервер перезапущен
- [ ] Протестирована отправка формы
- [ ] Письмо получено на указанный email
- [ ] Проверена обработка ошибок

---

## 📞 Поддержка

Если возникли проблемы:

1. Проверьте логи сервера в консоли
2. Убедитесь, что все переменные окружения заполнены
3. Проверьте настройки SMTP провайдера
4. Попробуйте другой провайдер для теста

---

## 📚 Дополнительные ресурсы

- [Nodemailer документация](https://nodemailer.com/about/)
- [Gmail SMTP настройки](https://support.google.com/mail/answer/7126229)
- [Yandex SMTP настройки](https://yandex.ru/support/mail/mail-clients.html)
- [SendGrid документация](https://docs.sendgrid.com/)
